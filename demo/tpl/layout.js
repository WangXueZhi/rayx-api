/* eslint-disable no-unused-vars */
import { axiosServer } from '../server/axiosServer'
import { axiosServerProxy } from '../server/axiosServerProxy'
import { responseWrap, errorResponse } from '../server/responseWrap'

export const install = (app) => {
  // _API_CODE_
}
